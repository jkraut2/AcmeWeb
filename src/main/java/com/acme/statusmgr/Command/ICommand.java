package com.acme.statusmgr.Command;

    /**
     * Interface for commands to be executed by Simple Executor. Every executor should execute.
     */
    public interface ICommand {

        void execute();
    }
